# G1 Scenario C1: Strict with sugar mask enabled
# Purpose: Baseline with heuristic sugar mask protection, smaller product space

rules: [R1, R2, R3, R6_methyl]
k_max: 2
halogens: [F, Cl, Br, I]

io:
  smiles_file: "data/input/glycoside_G1.smi"

engine_cfg:
  budget_mode: ops

sugar_cfg:
  mode: heuristic

rules_cfg:
  R2:
    sp3_CH2_flavanone: true
    allow_alpha_as_beta: false
    fallback:
      enable: false
